<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8" />
  <title>Card Shop Sato - ログイン</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      background: #f4f4f4;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .container {
      width: 360px;
      padding: 24px 20px 32px;
      background: #ffffff;
      border-radius: 16px;
      box-shadow: 0 10px 25px rgba(0,0,0,0.1);
      text-align: center;
    }

    .shop-name {
      font-size: 20px;
      font-weight: bold;
      margin-bottom: 24px;
    }

    .field {
      text-align: left;
      margin-bottom: 16px;
    }

    .field label {
      font-size: 12px;
      display: block;
      margin-bottom: 4px;
      color: #555;
    }

    .field input {
      width: 100%;
      padding: 10px;
      border-radius: 8px;
      border: 1px solid #ccc;
      font-size: 14px;
      box-sizing: border-box;
    }

    .login-button {
      width: 100%;
      padding: 10px;
      margin-top: 8px;
      border: none;
      border-radius: 999px;
      background: #2d7ff9;
      color: #fff;
      font-size: 14px;
      font-weight: bold;
      cursor: pointer;
    }

    .login-button:hover {
      opacity: 0.9;
    }

    .signup-link {
      margin-top: 16px;
      font-size: 12px;
      color: #555;
    }

    .signup-link a {
      color: #2d7ff9;
      text-decoration: none;
      font-weight: bold;
      margin-left: 4px;
    }

    .signup-link a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="shop-name">Card Shop Sato</div>

    <div class="field">
      <label for="email">メールアドレス</label>
      <input id="email" type="email" placeholder="example@mail.com" />
    </div>

    <div class="field">
      <label for="password">パスワード</label>
      <input id="password" type="password" placeholder="••••••••" />
    </div>

    <button class="login-button">ログイン</button>

    <div class="signup-link">
      初めての方はこちら
      <a href="#">会員登録</a>
    </div>
  </div>
</body>
</html>
